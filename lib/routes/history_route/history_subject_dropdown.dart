import 'package:flutter/material.dart';

class HistorySubjectDropdown extends StatelessWidget {
  const HistorySubjectDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
